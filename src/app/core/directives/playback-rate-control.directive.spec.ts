import { PlaybackRateControlDirective } from './playback-rate-control.directive';

describe('PlaybackRateControlDirective', () => {
  it('should create an instance', () => {
    const directive = new PlaybackRateControlDirective();
    expect(directive).toBeTruthy();
  });
});
